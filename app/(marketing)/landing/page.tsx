import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function LandingPage() {
  return <PlaceholderPage eyebrow="Marketing" title="Landing splash" description="V2-styled compatibility route for the old image splash landing page." chips={["Parity route"]} />;
}
